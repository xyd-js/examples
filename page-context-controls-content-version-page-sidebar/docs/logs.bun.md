---
title: Logs (Bun)
---

# Logs — Bun

## Usage

Use the `logger` module — this content comes from `docs/logs.bun.md`,
swapped onto the SAME URL via `?runtime=bun`.

## Notes

Deep links (`/docs/logs?runtime=bun`) render this variant immediately.
