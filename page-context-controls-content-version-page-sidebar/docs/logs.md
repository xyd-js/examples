---
title: Logs
---

# Logs — Angular

## Usage

Use the injected `Logger` service (the page's own content — the default
version).

## Notes

Pick **Bun** above the TOC: same URL, `?runtime=bun`, swapped content.
