---
title: Overview
---

# Overview

This example shows **nested navigation** on header anchors and tabs via
`dropdownMenu`, opened on `hover` (default) or `click`.

- The **Products** header anchor opens on hover and has a multi-level submenu
  (`Browser SDK → Install / Methods`).
- The **Company** header anchor opens on click.
- The **API Reference** tab opens on hover.
